# Google Analytics
# Get your GA4 Measurement ID from https://analytics.google.com/
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Google Site Verification
# Get your verification code from https://search.google.com/search-console
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your-verification-code-here

# Canonical host (avoid redirect loops between apex and www)
# Set to your preferred domain, e.g. "www.healthcalc.xyz"
# If your Vercel project only uses the apex domain, set this to the apex instead.
NEXT_PUBLIC_CANONICAL_HOST=www.healthcalc.xyz

# Sentry Error Monitoring
# Get your Sentry DSN from https://sentry.io/settings/projects/
# Format: https://[key]@[organization].ingest.sentry.io/[project-id]
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-key@your-org.ingest.sentry.io/your-project-id

# Optional: Sentry organization and project (for source map uploads)
SENTRY_ORG=your-organization-slug
SENTRY_PROJECT=your-project-name

# Optional: Enable Sentry in development (defaults to false)
# NEXT_PUBLIC_SENTRY_DEV_ENABLED=true

# ============================================================================
# NEWSLETTER / EMAIL MARKETING
# Configure ONE of the following email providers for newsletter subscriptions
# ============================================================================

# Option 1: Mailchimp
# Get your API key from https://mailchimp.com/help/about-api-keys/
# Find your Audience ID in Audience > Settings > Audience name and defaults
# MAILCHIMP_API_KEY=your-api-key-us1
# MAILCHIMP_AUDIENCE_ID=your-audience-id

# Option 2: ConvertKit
# Get your API key from https://app.convertkit.com/account_settings/developer_settings
# Create a form and get its ID from the form settings
# CONVERTKIT_API_KEY=your-api-key
# CONVERTKIT_FORM_ID=your-form-id

# Option 3: Resend
# Get your API key from https://resend.com/api-keys
# Create an audience and get its ID from the Audiences page
# RESEND_API_KEY=re_your-api-key
# RESEND_AUDIENCE_ID=your-audience-id
# Optional sender identity for contact-form outbound emails
# RESEND_FROM_EMAIL=HealthCheck Contact <noreply@healthcalc.xyz>

# Contact form recipient email (defaults to info@healthcalc.xyz)
# CONTACT_EMAIL=info@healthcalc.xyz

# ============================================================================
# MONETIZATION - ADVERTISING
# ============================================================================

# Google AdSense (loaded after advertising consent)
# This repo defaults to ca-pub-4505962980988232 unless overridden.
# Create ad units at https://www.google.com/adsense
# NEXT_PUBLIC_ADSENSE_PUBLISHER_ID=ca-pub-4505962980988232
# NEXT_PUBLIC_ADSENSE_SLOT_CONTENT=1234567891
# NEXT_PUBLIC_ADSENSE_SLOT_RESULT=1234567892
# NEXT_PUBLIC_ADSENSE_SLOT_SIDEBAR=1234567893
# NEXT_PUBLIC_ADSENSE_SLOT_RESPONSIVE=1234567894

# ============================================================================
# MONETIZATION - AFFILIATE PROGRAMS
# ============================================================================

# Amazon Associates
# Sign up at https://affiliate-program.amazon.com/
# AMAZON_ASSOCIATE_TAG=your-tag-20
# Optional hard gate for build pipelines. When true, prebuild fails if any
# placeholder direct partner links are still present in affiliates.ts.
# AFFILIATE_LINKS_STRICT=false

# ============================================================================
# SITE URL (used for CSRF protection)
# ============================================================================
# Set to your production domain for CSRF verification
NEXT_PUBLIC_SITE_URL=https://www.healthcalc.xyz

# ============================================================================
# INTERNATIONALIZATION (i18n)
# ============================================================================
# Comma-separated list of locales that are fully translated and safe to index.
# Defaults to "en". When unset, non-default locale routes are served but noindexed
# and excluded from hreflang alternates to avoid SEO issues with untranslated pages.
# I18N_INDEXABLE_LOCALES=en,es,fr,de,pt,zh

# ============================================================================
# SUBMISSION PERSISTENCE
# ============================================================================
# Storage driver:
# - "sqlite" for local/dev
# - "postgres" for managed database persistence
# If omitted, driver auto-detects: Postgres when DATABASE_URL is set, otherwise SQLite.
# SUBMISSIONS_DB_DRIVER=sqlite

# Optional strict mode:
# If true, API responses fail closed with 503 when submission persistence fails.
# SUBMISSIONS_PERSISTENCE_STRICT=false

# Retention window in days for newsletter/contact/embed submissions.
# Set to 0 to disable automatic cleanup.
# SUBMISSIONS_RETENTION_DAYS=365

# Optional interval between retention sweeps (milliseconds).
# SUBMISSIONS_RETENTION_SWEEP_MS=900000

# SQLite local path (used when SUBMISSIONS_DB_DRIVER=sqlite)
# SQLITE_DB_PATH=.data/healthcheck.sqlite

# Managed Postgres connection (preferred for production durability).
# SUBMISSIONS_POSTGRES_URL=postgres://user:password@host:5432/database
# DATABASE_URL=postgres://user:password@host:5432/database
#
# Optional: Saved results persistence (requires Clerk auth).
# If omitted, saved results fall back to SUBMISSIONS_POSTGRES_URL or DATABASE_URL.
# SAVED_RESULTS_POSTGRES_URL=postgres://user:password@host:5432/database
# SAVED_RESULTS_POSTGRES_MAX_CONNECTIONS=10
#
# Optional: Disable TLS/SSL for Postgres connections (not recommended in production).
# PGSSLMODE=disable

# ============================================================================
# AUTHENTICATION (Clerk) - Optional
# ============================================================================
# Sign up at https://clerk.com/ and create an application
# Development keys (non-production only):
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your-key
# CLERK_SECRET_KEY=sk_test_your-key
# Production keys (required before enabling auth in production):
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_your-key
# CLERK_SECRET_KEY=sk_live_your-key

# ============================================================================
# ENVIRONMENT
# ============================================================================
NODE_ENV=production
